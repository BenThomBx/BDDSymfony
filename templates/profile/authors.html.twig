{% extends "profile/base.html.twig" %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('css/authors.css') }}">{% endblock stylesheets %}

{% block main %}
    <main>
        <section class="section">
            {% for author in authors %}
                <div class="card">
                    <div class="card-text">
                        <h2>{{ author.fisrtName }}</h2>
                        <h2>{{ author.lastName }}</h2>
                        <h3>{{ author.deathDate | date('d/m/y')}}</h3>
                    </div>
                    {#                    <div class="card-image">#}
                    {#                        <img src="{{ books.image }}" alt="Image du livre">#}
                    {#                    </div>#}
                    <a class="link" href="{{ path('profile_author', {"id": author.id}) }}">Consulter la fiche auteur</a><br>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock main%}