{% extends "profile/base.html.twig" %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('css/books.css') }}">{% endblock stylesheets %}

{% block main %}
    <main>
        <section class="section">
            {% for book in books %}
                <div class="card">
                    <div class="card-text">
                        <h2>{{ book.title }}</h2>
                        {#  <h3>{{ book.author.fisrtname }} {{book.author.lastname}}</h3>#}
                    </div>
                    {#                    <div class="card-image">#}
                    {#                        <img src="{{ books.image }}" alt="Image du livre">#}
                    {#                    </div>#}
                    <a class="link" href="{{ path('profile_book', {"id": book.id}) }}">Détails du livre</a><br>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock main%}