{% extends "base.html.twig" %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/author.css') }}">
{% endblock stylesheets %}

{% block main %}
    <article class="author-detail">
        <div class="card-text">
                <h2>{{ author.fisrtname }}</h2>
                <h2>{{ author.lastname }}</h2>
                <h3>{{ author.deathDate | date('d/m/y')}}</h3>

            {#COMMENTAIRE #}
    {# boucle permettant d'afficher tous les livres qui sont reliés par une même clé étrangère à un même auteur #}
             {% for books in author.books %}

                <h2>{{ books.title }}</h2>

            {% endfor %}
                                       {#  <ul>
                                       //     {% for key, user in users %}
                                       //         <li>{{ key }}: {{ user.username|e }}</li>
                                            {% endfor %}
                                        </ul>
                                       //$books=new Arraycollection()? //
                                       #}




                    {#                    <div class="card-image">#}
                    {#                        <img src="{{ books.image }}" alt="Image du livre">#}
                    {#                    </div>#}


        </div>

    </article>
{% endblock main%}

