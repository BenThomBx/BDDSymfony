{% extends "base.html.twig" %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('css/authors.css') }}">{% endblock stylesheets %}

{% block main %}
    <main>
        <section class="section">
            {% for authors in authors %}
                <div class="card">
                    <div class="card-text">
                        <h2>{{ authors.fisrtName }}</h2>
                        <h2>{{ authors.lastName }}</h2>
                        <h3>{{ authors.deathDate | date('d/m/y')}}</h3>
                    </div>
                    {#                    <div class="card-image">#}
                    {#                        <img src="{{ books.image }}" alt="Image du livre">#}
                    {#                    </div>#}
                    <a class="link" href="{{ path('authors', {"id": authors.id}) }}">En savoir plus</a>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock main%}