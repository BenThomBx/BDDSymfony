<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block metatitle %}La bibliothèque pour tous{% endblock metatitle%}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('asset/css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/article.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/author.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/base.css') }}">

</head>
<body>
<header class="header">
    <h1>BIBLIOTHÈQUE DES PASSIONNÉS administrateur</h1>
    <nav class="navbar">
        <ul class="menu">
            <li class="main-menu-element">
                <a href="{{ path('admin_dashboard') }}">Accueil</a>
            </li>
            <li class="main-menu-element">
                <a href="{{ path('admin_books') }}">Les livres</a>
            </li>
            <li class="main-menu-element">
                <a href="{{ path('admin_authors') }}">Les auteurs</a>
            </li>
            <li class="main-menu-element">
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            </li>
        </ul>
        {# EXO 13/12/21 10:14:
        // Créez un moteur de recherche pour les livres avec :
        // 5 - créez un formulaire html dans votre menu et récupérez le mot recherché dans la page (de recherche pour
        //     trouver les livres reliés et les afficher)
        //COMMENTAIRE 13/12/21 Moteur de Recherche
        //on crée le formulaire qui permettra d'aller récupérer l'objet de la recherche. (Input "q") et de soumettre
        //celui-ci en entête de page#}

        <p>Vous êtes connecté en tant que {{ app.user.email }}</p>

        <form action="{{path("admin_book_search")}}">
            <input type="text" name="q">
            <input type="submit">
        </form>
    </nav>
</header>

<main>

    {% block main %}{% endblock main %}


</main>
<footer class="footer">
    <div class="footer-group">
        <div class="footer-elements">
            <ul>
                <li><span>A propos</span></li>
                <li>Equipe</li>
                <li>Avis clients</li>
                <li>Conditions générales</li>
                <li>Mentions légales</li>
            </ul>
        </div>
    </div>
</footer>
</body>
</html>